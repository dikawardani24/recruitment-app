# Optional dependencies for the local BERT resume-NER extractor
# (ATS_EXTRACT__NER=true). Install separately — adds ~2GB (torch CPU).
# CPU build:
#   pip install --index-url https://download.pytorch.org/whl/cpu torch
#   pip install -r requirements-ml.txt
torch
transformers
numpy
